/**
 * Migration Mocks for Testing
 *
 * Mocks for the automatic migration system to prevent actual migrations
 * from running during tests.
 */

export const setupMigrationMocks = () => {
  // This function is now a no-op since migration mocks are handled
  // directly in test setup files using jest.mock()
};
