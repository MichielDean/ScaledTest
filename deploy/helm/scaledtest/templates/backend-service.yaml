apiVersion: v1
kind: Service
metadata:
  name: {{ include "scaledtest.fullname" . }}-backend
  labels:
    {{- include "scaledtest.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (eq .Values.backend.service.type "NodePort") .Values.backend.service.nodePort }}
      nodePort: {{ .Values.backend.service.nodePort }}
      {{- end }}
    - port: {{ .Values.backend.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
      {{- if and (eq .Values.backend.service.type "NodePort") .Values.backend.service.grpcNodePort }}
      nodePort: {{ .Values.backend.service.grpcNodePort }}
      {{- end }}
  selector:
    {{- include "scaledtest.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: backend
